import React, { useState, useEffect, useRef } from "react";

function BuildData() {
  const DATA_TYPE = {
    TITLE: 0,
    TEXT: 1,
    IMAGE: 2,
  };

  return <div className="canvascontainer ">BuildData</div>;
}

export default BuildData;
